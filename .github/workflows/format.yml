name: Formatting

on:
  pull_request:
  push:

jobs:
  fourmolu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: haskell-actions/run-fourmolu@v12
      with:
        version: "0.12.0.0"
        pattern: |
          **/*.hs
